# API Specification

**Project:** Event Management System (Model Set 3)  
**Base URL:** `http://localhost:3000`  
**Swagger UI:** `http://localhost:3000/api`  

---

## 📌 Standard Response Format

ทุก Endpoint ในระบบจะตอบกลับด้วยรูปแบบ JSON มาตรฐานดังนี้

```json
{
  "success": true,
  "message": "string",
  "data": "object | array | null"
}
```
👤 1. Auth & Participants (ระบบผู้ใช้งาน)
1.1 สมัครสมาชิกใหม่ (Register)

Method: POST

Endpoint: /auth/register

Description: สร้างบัญชีผู้ใช้งานใหม่ (Role: PARTICIPANT)

Request Body:

```json
{
  "username": "johndoe",
  "password": "password123",
  "email": "john@example.com",
  "firstName": "John",
  "lastName": "Doe",
  "phoneNumber": "0812345678"
}
```
Response (201 Created): คืนค่าข้อมูลโปรไฟล์ (ไม่รวมรหัสผ่าน)

1.2 เข้าสู่ระบบ (Login)

Method: POST

Endpoint: /auth/login

Request Body:

```json
{
  "username": "johndoe",
  "password": "password123"
}
```
Response (200 OK): คืนค่าข้อมูลโปรไฟล์ (ไม่รวมรหัสผ่าน)

1.3 ดึงข้อมูลผู้ใช้งานทั้งหมด

Method: GET

Endpoint: /participants

Response (200 OK): คืนค่า Array ของผู้ใช้งานทั้งหมด

1.4 ดึงข้อมูลผู้ใช้งานตาม ID

Method: GET

Endpoint: /participants/:id

Response (200 OK): คืนค่า Object ข้อมูลผู้ใช้งาน

1.5 แก้ไขข้อมูลผู้ใช้งาน

Method: PATCH

Endpoint: /participants/:id

Description: แก้ไขข้อมูลบางส่วน (ไม่อนุญาตให้อัปเดต username และ password ผ่าน API นี้)

Request Body (Example):

```json
{
  "phoneNumber": "0999999999",
  "firstName": "Johnny"
}
```
Response (200 OK): คืนค่าข้อมูลโปรไฟล์ที่อัปเดตแล้ว

1.6 ลบผู้ใช้งาน

Method: DELETE

Endpoint: /participants/:id

Response (200 OK): ลบสำเร็จ (data เป็น null)

📅 2. Events (ระบบกิจกรรม)
2.1 สร้างกิจกรรมใหม่

Method: POST

Endpoint: /events

Request Body:

```json
{
  "name": "NestJS Workshop",
  "description": "เรียนรู้การสร้าง API ด้วย NestJS",
  "startTime": "2026-03-01T09:00:00Z",
  "endTime": "2026-03-01T16:00:00Z",
  "location": "Bangkok, Thailand",
  "capacity": 50,
  "status": "UPCOMING"
}
```
Response (201 Created): คืนค่า Object ข้อมูลกิจกรรมที่ถูกสร้าง

2.2 ดึงข้อมูลกิจกรรมทั้งหมด

Method: GET

Endpoint: /events

Response (200 OK): คืนค่า Array ของกิจกรรมทั้งหมด

2.3 ดึงข้อมูลกิจกรรมตาม ID

Method: GET

Endpoint: /events/:id

Response (200 OK): คืนค่า Object ของกิจกรรม

2.4 แก้ไขข้อมูลกิจกรรม

Method: PATCH

Endpoint: /events/:id

Request Body (Example):

```json
{
  "status": "ONGOING",
  "capacity": 60
}
```
Response (200 OK): คืนค่าข้อมูลกิจกรรมที่อัปเดตแล้ว

2.5 ลบกิจกรรม

Method: DELETE

Endpoint: /events/:id

Response (200 OK): ลบสำเร็จ (data เป็น null)

🎟️ 3. Reservations (ระบบการจอง)
3.1 จองกิจกรรม

Method: POST

Endpoint: /reservations

Description: Participant ทำการจองกิจกรรม (ระบบจะเช็คที่นั่งเต็มและการจองซ้ำอัตโนมัติ)

Request Body:

```json
{
  "userId": "uuid-ของ-user",
  "eventId": "uuid-ของ-event"
}
```
Response (201 Created): คืนค่า Object ข้อมูลการจองที่ได้รับการยืนยัน (สถานะ CONFIRMED)

3.2 ดูประวัติการจองของผู้ใช้

Method: GET

Endpoint: /reservations/user/:userId

Response (200 OK): คืนค่า Array ประวัติการจองทั้งหมดของผู้ใช้ที่ระบุ

3.3 ยกเลิกการจอง

Method: PATCH

Endpoint: /reservations/:id/cancel

Description: เปลี่ยนสถานะการจองเป็น CANCELLED และคืนที่นั่งให้ Event

Request Body:

```json
{
  "userId": "uuid-ของ-user" 
}
```
(หมายเหตุ: ต้องส่ง userId มาเพื่อยืนยันสิทธิ์ความเป็นเจ้าของการจอง)

Response (200 OK): คืนค่าข้อมูลการจองที่อัปเดตสถานะแล้ว 